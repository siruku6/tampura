# Docker build arguments
# -----------------------------------------------
# ホストの UID/GID に合わせる（生成ファイルの権限ズレ防止）
USER_NAME=tampura
UID=1000
GID=1000

# 1: 起動時に pyproject.toml 変更を検知して uv で再同期
# 0: Dockerfile ビルド時の依存をそのまま使う（推奨: 毎回の再インストール回避）
TAMPURA_AUTO_SYNC=0

# Jupyter Notebook
JUPYTER_PORT=8888
